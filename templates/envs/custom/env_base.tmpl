{{/* docker-compose_base.tmpl */}}
{{ define "env" }}
# --- Global configuration ---
NETWORK=custom
HEALTHCHECK_CMD="(curl -s -o /dev/null -w \"%{http_code}\" -X GET {{.ConsensusApiURL}}/eth/v1/node/health | grep -q 200) || exit 1"
{{template "execution" .}}
{{template "consensus" .}}
{{template "validator" .}}
{{ end }}